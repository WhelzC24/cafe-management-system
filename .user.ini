; Cozy Corner Café — PHP user overrides for CGI/FPM environments
; Keeps upload limits consistent with .htaccess values

upload_max_filesize = 20M
post_max_size = 24M
memory_limit = 256M
max_execution_time = 60
